export type LoginType = {
  email: string;
  password: string;
};

export type RegisterType = {
  name: string;
  password: string;
  confirmPassword: string;
  email: string;
  bio: string;
  institution: string;
};
